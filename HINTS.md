# Hints

<!-- User-supplied behavior directives. The skill reads this at session start
     and respects any constraint named here. Examples (Tenstorrent):
     - Optimization constraints / focus areas (e.g., "Prefer bfloat8_b over bfloat16")
     - Strategies to try or avoid       (e.g., "Do not use sharding", "Keep tensors in L1")
     - Accuracy floor                    (e.g., "PCC must stay >= 0.999")
     - Agent behavior controls           (e.g., "Stop after 5 iterations")
     - Dependency policies               (e.g., "Do not install new packages")
     - Environment constraints           (e.g., "Device profiler build unavailable",
                                               "Target is Blackhole (p150)",
                                               "ARCH_NAME=wormhole_b0",
                                               "tt-metal at /opt/tt-metal")

     The skill's own protocol (iteration steps, stall handling, profiler
     fallback, stopping rules) lives in SKILL.md — do not duplicate it here.
-->
